import React, { Component } from 'react';

class RoundDown extends Component {
    render() {
        return (
            <div>
                <button className="round">Round Down to nearest 15 minutes</button>
            </div>
        )
    }
}

export default RoundDown;